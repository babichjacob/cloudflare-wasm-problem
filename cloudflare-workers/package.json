{
  "name": "cloudflare-workers",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler publish"
  },
  "dependencies": {
    "utils-in-rust": "workspace:*"
  },
  "devDependencies": {
    "wrangler": "^2.0.29"
  }
}
